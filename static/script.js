function toggleMenu() {
    var navLinks = document.getElementById('navbar').getElementsByTagName('ul')[0];
    navLinks.classList.toggle('active');
}
document.addEventListener('DOMContentLoaded', function() {
    // Nothing needed here, the 'typing' class is applied in the HTML
});
